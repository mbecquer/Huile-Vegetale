{% extends "base.html.twig" %}

{% block title %}
	{{title}}
{% endblock %}
{% block body %}
	<h1 class="text-4xl text-center mt-10 text-green">{{title}}</h1>

	{% for article in articles %}
		<div
			class="flex flex-wrap  mx-52">
			<!--article-->
			<div class="flex flex-wrap my-5 rounded-lg shadow-lg p-5 w-full">

				<div class="image-article ">
					<img class="w-72 h-72" src="{{article.image }}"></div>

				<div class="text-article mx-5  ">
					<h2>{{article.title}}</h2>
					<p class="text-green">Hier à 14h50</p>
					<p class="text-lg">{{article.content}}</p>

				</div>
			</div>
		</div>
	{% endfor %}

{% endblock %}
