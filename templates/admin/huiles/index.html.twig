{% extends 'base.html.twig'%}

{% block title 'Administration'%}

{% block body %}
	<h2 class="text-4xl text-center mt-10 mb-5 text-green">Administration</h2>

	<div class="mx-28">
		{% for message in app.flashes('success')  %}

			<div class="bg-green-200 w-full text-center text-3xl p-2 mb-2 rounded-lg">
				{{message}}
			</div>

		{% endfor %}


		<a href="{{path('admin_huiles_create')}}" class="btn">
			Ajouter
		</a>


		<table class="w-full my-5  table-striped">
			<thead class="border-b-2 font-bold text-2xl">
				<tr>
					<th>ID</th>
					<th>NOM</th>
					<th>DESCRIPTION</th>
					<th>IMAGE</th>
					<th>CAPACITE</th>
					<th>PRIX</th>
					<th>actions</th>
				</tr>
			</thead>
			<tbody>
				{% for huile in huiles %}
					<tr class="text-center  cursor-pointer text-xl border-b-2">
						<td>{{huile.id}}</td>
						<td>{{huile.name}}</td>
						<td>{{huile.description}}</td>
						<td><img class="mx-auto" src="{{huile.image}}"></td>
						<td>{{huile.capacity}}
							ml</td>
						<td>{{huile.price}}
							€</td>
						<td>
							<a href="{{path('admin_huiles_edit', { id: huile.id })}}" class="btn">Modifier</a>

							<a href="{{path('admin_huiles_delete', { id: huile.id })}}" class="btn">Supprimer</a>


						</td>


					</tr>

				{% endfor %}
			</tbody>
		</table>
	</div>
{% endblock %}
